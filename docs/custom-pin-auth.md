# Email-PIN login removed

The application no longer uses the approved-staff email-PIN system. The login screen, session checks, PIN endpoints, and identity presence display have been removed.

See [public access deployment notes](public-access.md) for current configuration and cleanup of the retired mailer. The former setup instructions remain available in Git history.
